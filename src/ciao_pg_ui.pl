:- module(_, [], []).

% EMPTY (this module is implemented in JavaScript)

% TODO: add some prop like 'can_run_in_js_client'; that is emulated in wui_html or executed in playground JS

% Set value of buffer BufName
% :- suspendable(set_buf(const, string)).

% Focus the buffer BufName
% :- suspendable(focus_buf(const)).

% Select mode of buffer
% :- suspendable(buf_mode(const, const)).

% Set value of an HTML view
% :- suspendable(set_html_view(const, string)).

% Show/hide in-progress indicator
% :- suspendable(show_in_progress(json)).

% Show modal dialog
% :- suspendable(show_modal(tagval_list)).

% Setup playground
% :- suspendable(pg_setup(string)).

% Set main toolbar
% :- suspendable(set_main_toolbar(json)).

